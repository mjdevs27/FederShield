import NodeSimulation from '@/components/NodeSimulation';

export const metadata = {
    title: 'Node Simulation | FederShield',
    description: 'Interactive Node-to-Server Federated Learning Simulation',
};

export default function NodeSimulationPage() {
    return <NodeSimulation />;
}
