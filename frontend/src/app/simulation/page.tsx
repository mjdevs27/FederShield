'use client';

import FederatedSimulation from '@/components/FederatedSimulation';

export default function SimulationPage() {
    return (
        <main className="min-h-screen bg-[#050505]">
            <FederatedSimulation />
        </main>
    );
}
